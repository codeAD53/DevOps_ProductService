apiVersion: v1
kind: Service

metadata:
  name: mongo-headless

spec:
  clusterIP: None #headless service

  selector:
    app: mongo
  
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP