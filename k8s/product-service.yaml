apiVersion: v1
kind: Service

metadata:
  name: product-server-service

spec:
  selector:
    app: product-server

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
    
  type: NodePort
